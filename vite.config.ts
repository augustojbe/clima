import { defineConfig } from 'vite';

export default defineConfig({
  // Se o repositório no GitHub se chamar 'clima', o base deve ser '/clima/'
  // Se for uma User Page (ex: username.github.io) ou tiver domínio customizado, altere para '/'
  base: '/clima/',
});
